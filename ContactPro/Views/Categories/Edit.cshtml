@model ContactPro.Models.Category

@{
    ViewData["Title"] = "Edit";
}



<div class="container">
    <div class="card drop-shadow p-5">
        @*<h1>Delete Category</h1>*@

        <h1 class="text-center">Edit Category</h1>
        <hr />

        <div class="row">
            <div class="col-12">
                <form asp-action="Edit" class="mt-3">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <input type="hidden" asp-for="Id" />
                    <input type="hidden" asp-for="AppUserID" />
                    <div class="form-group">
                        <div class="row">
                            <div class="col-4 text-end">
                                <label asp-for="Name" class="form-label">Enter The Category Name:</label>
                            </div>
                            <div class="col-6">
                                <input asp-for="Name" class="form-control" />
                                <span asp-validation-for="Name" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                    <div class="row mt-3">
                        <div class="col-6 text-end">
                            <a class="btn btn-outline-primary rounded-pill" asp-action="Index"><i class="bi bi-chevron-left"></i> Back to List</a>
                        </div>
                        <div class="col-6">
                            <input type="submit" value="Save" class="btn btn-primary rounded-pill btnlinks" />
                        </div>

                    </div>
                </form>
            </div>
        </div>

        @*<div>
        <a class="btn btn-primary rounded-pill" asp-action="Index">Back to List</a>
        </div>

        <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <div class="row">
        <div class="col-6 text-end">
        <a class="btn btn-outline-primary rounded-pill" asp-action="Index"><i class="bi bi-chevron-left"></i> Back to List</a>
        </div>
        <div class="col-6">
        <input type="submit" value="Save" class="btn btn-primary rounded-pill btnlinks" />
        </div>

        </div>
        </form>
        </div>*@
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }
